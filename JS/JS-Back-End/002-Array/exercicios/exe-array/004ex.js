// 4 -> Verifique se um elemento específico existe no array.
const palavras = ['spray', 'limit', 'elite', 'exuberant', 'destruction', 'present'];

function procurandoElemento(elemento){
    const elementoEncontrado = palavras.filter(palavras === elemento)
    console.log(elementoEncontrado)

}
procurandoElemento('spray')