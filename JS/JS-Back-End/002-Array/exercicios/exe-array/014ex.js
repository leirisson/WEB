// 10: Transponha uma matriz (converta linhas em colunas e vice-versa).

const matriz = [[],[],[]]

function nunmerosAleatorios(min, max){
    min = Math.ceil(min)
    max = Math.floor(max)
    return Math.floor(Math.random() * (max-min) + min)
}
